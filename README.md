# Graphs

### Some snippets
```
bool safe(int x,int y,int n,int m){
    return x>=0 && x<n && y>=0 && y<m;
}

// Setting all value in array lvl to 0
int lvl[101][101];
memset(lvl,0,sizeof(lvl));

queue<pair<int,int>>Q;
Q.push(make_pair(i,j));
```
